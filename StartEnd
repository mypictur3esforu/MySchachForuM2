import javax.swing.*;

public class StartEnd {
    public static String winner = "";

    public static void StartGame(){
        UI.bigPanel.remove(UI.startScreen);
        UI.CreateSideBar();
        UI.bigPanel.add(UI.field);
        UI.bigPanel.add(UI.sidebar);
        UI.bigPanel.setVisible(true);
        UI.frame.setVisible(true);
    }

    public static void NewGame(){
        UI.bigPanel.remove(UI.winner);
        //Court.BordDefinition();
        UI.bigPanel.add(UI.field);
        //UI.field.setVisible(true);
        //UI.bigPanel.setVisible(true);
        UI.frame.setVisible(true);
    }

    public static void End(){
        //UI.frame.remove(UI.field);
        //UI.EndScreen(Usefull.OpposingColor(WhoseTurn.turn));
    }
}
